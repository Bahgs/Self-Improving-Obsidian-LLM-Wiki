# Agent Runtime Compatibility

This folder is a placeholder for agent runtime compatibility.

The public, human-readable skill documentation lives in `Skills/`.

If your agent runtime installs hidden skills or caches here, do not commit private credentials, local caches, or generated runtime state.

